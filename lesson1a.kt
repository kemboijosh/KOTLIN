fun main(){
    // A variable is a container tht stores data/values
    // In kotlin we have two main types of initializers i.e var and val
    // val => immutable (unchangeable) but var => mutable(changeable)

    val name = "Joshua Kemboi"string => // a series of character inside of quotes
    val number1 = 5 //interger -> whole number
    val number2 = 40.5 // Double -> number with decimal
    val letter = "y" // char ->it is a single letter of the alphabet
    val isRegistered = true //boolean -> it evaluates whether true or false 

    println(name)
    println(number1)
    println(number2)
    println(letter)
    println(isRegistered)
}